---
# Actions to run every restart.
#
alias: shutdown
trigger:
  platform: homeassistant
  event: shutdown
action:
  - service: mqtt.publish
    data:
      topic: "hass/status"
      payload: "offline"
      retain: true
