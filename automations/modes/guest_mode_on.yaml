---
alias: guest_mode_on
initial_state: true
trigger:
  - platform: state
    entity_id:
      - input_boolean.guest_mode
    from: 'off'
    to: 'on'
action:
  - service: script.say
    data_template:
      message: !include ../../templates/speech/guest_mode_on.yaml
