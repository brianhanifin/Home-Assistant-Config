## Control Panel
#######################
panel_control:
  entities:
    - device_tracker.brian
    - device_tracker.nerene
    - binary_sensor.glowforge_window
    - binary_sensor.garage_entry_door
    - binary_sensor.garage_door
    - sensor.low_battery

    - group.brian_alarm_clock
    - group.nerene_alarm_clock

    - group.panel_control_modes
    - group.panel_control_scenes
  name: Control Panel
  icon: mdi:file-document-box
  control: hidden
  view: no

panel_control_modes:
  entities:
    #- input_boolean.alert_mode
    - input_boolean.bedroom_light_sync_enabled
    - input_boolean.dusk_dimmers_enabled
    - input_boolean.flashing_lights_enabled
    - input_boolean.guest_mode
    - input_boolean.presence_lighting_enabled
    - input_boolean.school_mode
    - input_boolean.speech_notifications
    - input_boolean.text_notifications
    - input_boolean.vacation
    - input_boolean.lastmsg
    - sensor.last_message
  name: House Modes
  icon: mdi:power
  control: hidden
  view: no

panel_control_scenes:
  entities:
    - scene.arrive_at_home
    - scene.goodbye
    - scene.good_night
  name: Scenes
  icon: mdi:file-document-box
  control: hidden
  view: no