## Control Panel
#######################
panel_control:
  entities:
    - device_tracker.brian
    - device_tracker.nerene
    - binary_sensor.glowforge_window
    - binary_sensor.garage_door
    - group.wakeup_brian
    - group.wakeup_nerene
    #- sensor.hline
    - group.panel_control_overrides
  name: Control Panel
  icon: mdi:file-document-box
  control: hidden
  view: no
panel_control_bed_presence:
  entities:
    - device_tracker.brian
    - device_tracker.nerene
    - binary_sensor.brians_in_bed
    - binary_sensor.nerenes_in_bed
    - binary_sensor.glowforge_window
    - binary_sensor.garage_door
    - group.wakeup_brian
    - group.wakeup_nerene
    #- sensor.hline
    - group.panel_control_overrides
  name: Control Panel
  icon: mdi:file-document-box
  control: hidden
  view: no

panel_control_overrides:
  entities:
    - input_boolean.guest_mode
    - input_boolean.illumination_lighting_mode
    - input_boolean.movie_night
    - input_boolean.motion_detection
    - input_boolean.motion_detection_familyroom
    - input_boolean.motion_detection_livingroom
    - input_boolean.motion_detection_kitchen
    - input_boolean.motion_detection_playroom
    - input_boolean.speech_notifications
    - input_boolean.text_notifications
    - input_boolean.time_based_dimmers_enabled
    - input_boolean.vacation
    - scene.goodbye
    - scene.good_night
  name: Mode Overrides
  icon: mdi:home-alert
  control: hidden
  view: no