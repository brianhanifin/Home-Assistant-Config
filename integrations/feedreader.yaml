---
# Add an RSS/Atom feed reader that polls feeds every hour and
# sends new entries into the event bus.
#
# https://www.home-assistant.io/components/feedreader/
#
feedreader: !include_dir_merge_named ../integration_components/feedreader
