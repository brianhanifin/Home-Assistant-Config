---
# The recorder component is responsible for storing details in
# a database, which then are handled by the history component.
#
# https://www.home-assistant.io/components/recorder/
#
recorder: !include_dir_merge_named ../../integration_entities/recorder
