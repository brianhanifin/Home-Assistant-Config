---
# The logger component lets you define the level of logging activities
# in Home Assistant.
#
# Possible log severity levels, listed in order from most severe
# to least severe, are:
#
# * critical
# * fatal
# * error
# * warning
# * warn
# * info
# * debug
# * notset
#
# https://www.home-assistant.io/components/logger/
#
logger:
  #default: error
  default: warn
  #default: info
  #logs:
    # custom_components.weatheralerts: debug
    # homeassistant.components.camera: debug
    # homeassistant.components.ffmpeg: debug
    # custom_components.camera.amcrest: debug
    # custom_components.camera.reolink_dev: debug

