---
# Keeps track which remotes are in your environment, their state
# and allows you to control them.
#
# https://www.home-assistant.io/components/remote/
#
remote: !include_dir_list /config/integration_components/remotes
