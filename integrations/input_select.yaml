---
# The input_select component allows the user to define a list of
# values that can be selected via the frontend and can be used
# within conditions of automation. When a user selects a new item,
# a state transition event is generated. This state event can be
# used in an automation trigger.
#
# https://www.home-assistant.io/components/input_select/
#
input_select: !include_dir_merge_named ../integration_components/input_selects
