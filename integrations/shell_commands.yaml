---
# This component can expose regular shell commands as services. Services
# can be called from a script or in automation. Shell commands aren’t
# allowed for a camel-case naming, please use lowercase naming only and
# separate the names with underscores.
#
# This loads up my custom Shell Commands.
#
# https://www.home-assistant.io/components/shell_command/
#
shell_command: !include_dir_merge_named ../shell_commands
