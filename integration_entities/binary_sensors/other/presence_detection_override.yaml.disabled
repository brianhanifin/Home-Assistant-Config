---
platform: template
sensors:
  presence_detection_override:
    friendly_name: Presence Detection Override
    entity_id: input_boolean.presence_detection_mode
    value_template: "{{ is_state('input_boolean.presence_detection_mode','off') }}"
