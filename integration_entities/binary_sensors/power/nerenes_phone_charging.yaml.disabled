---
platform: template
sensors:
  nerenes_phone_charging:
    #entity_id: device_tracker.life360_nerene
    value_template: "{{ is_state_attr('device_tracker.life360_nerene', 'battery_charging', true) }}"
