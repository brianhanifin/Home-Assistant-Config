---
platform: template
sensors:
  irrigation_zone1:
    friendly_name: Irrigation Zone 1
    entity_id: sensor.irrigation_zone1_remaining
    value_template: "{{ states('sensor.irrigation_zone1_remaining')|int > 0 }}"
