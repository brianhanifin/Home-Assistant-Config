---
platform: template
sensors:
  irrigation_zone2:
    friendly_name: Irrigation Zone 2
    entity_id: sensor.irrigation_zone2_remaining
    value_template: "{{ states('sensor.irrigation_zone2_remaining')|int > 0 }}"
