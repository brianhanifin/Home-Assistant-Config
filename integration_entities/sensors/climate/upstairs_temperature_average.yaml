---
platform: average
name: Upstairs temperature average
# start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0).replace(microsecond=0) }}'
# end: '{{ now() }}'
entities:
  - sensor.bedroom_temperature
  - sensor.lucas_room_temperature
  - sensor.kyles_bedroom_temperature
  - sensor.guest_room_temperature
  - sensor.upstairs_bathroom_temperature
