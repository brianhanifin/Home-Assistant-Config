---
platform: template
sensors:
  today_rain:
    entity_id: weather.weatherbit
    value_template: "{{ state_attr('weather.weatherbit','forecast')[0].precipitation|default('0')|float }}"
    friendly_name: Today's rain
    #unit_of_measurement: "%"
