---
platform: template
sensors:
  today_rain:
    entity_id: weather.openweathermap
    value_template: "{{ state_attr('weather.openweathermap','forecast')[0].precipitation|default('0')|int }}"
    friendly_name: Today's rain
    #unit_of_measurement: "%"
