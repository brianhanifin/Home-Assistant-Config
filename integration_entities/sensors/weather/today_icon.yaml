---
platform: template
sensors:
  today_icon:
    entity_id: weather.weatherbit_santee
    friendly_name: "Today's forecast"
    icon_template: "{{ 'mdi:weather-' ~ states('weather.weatherbit_santee')|replace('clear-','')|replace('partly','partly-') }}"
    value_template: "{{ states('weather.weatherbit_santee') }}"
