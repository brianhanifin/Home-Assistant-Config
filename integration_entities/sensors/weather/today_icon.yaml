---
platform: template
sensors:
  today_icon:
    entity_id: weather.openweathermap
    friendly_name: "Today's forecast"
    icon_template: "{{ 'mdi:weather-' ~ states('weather.openweathermap')|replace('clear-','')|replace('partly','partly-') }}"
    value_template: "{{ states('weather.openweathermap') }}"
