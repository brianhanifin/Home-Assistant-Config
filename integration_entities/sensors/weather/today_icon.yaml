---
platform: template
sensors:
  today_icon:
    #entity_id: weather.santee
    friendly_name: "Today's forecast"
    icon_template: "{{ 'mdi:weather-' ~ states('weather.santee')|replace('clear-','')|replace('partly','partly-') }}"
    value_template: "{{ states('weather.santee') }}"
