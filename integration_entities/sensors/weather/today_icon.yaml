---
platform: template
sensors:
  today_icon:
    entity_id: weather.weatherbit
    friendly_name: "Today's forecast"
    icon_template: "{{ 'mdi:weather-' ~ states('weather.weatherbit')|replace('clear-','')|replace('partly','partly-') }}"
    value_template: "{{ states('weather.weatherbit') }}"
