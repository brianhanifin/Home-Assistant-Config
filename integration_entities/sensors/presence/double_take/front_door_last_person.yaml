---
platform: template
sensors:
  front_door_last_person:
    friendly_name: "Front Door Last Person"
    icon_template: "mdi:face-recognition"
    value_template: |
      {%- set match = state_attr("sensor.double_take_front_door","matches")[0] %}
      {{ match["name"] if match["confidence"] > 96 }}
