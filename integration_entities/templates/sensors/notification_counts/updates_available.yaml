---
unique_id: updates_available
name: Updates available
icon: mdi:update
state_class: total
state: |
  {{ states.update | selectattr("state","eq","on") | sort(attribute="name") | map(attribute="entity_id") | list | count }}
attributes:
  names: |
    {{ states.update | selectattr("state","eq","on") | sort(attribute="name") | map(attribute="name") | sort | list | replace(" Update", "") }}
  entities: |
    {{ states.update | selectattr("state","eq","on") | sort(attribute="name") | map(attribute="entity_id") | sort | list }}
