---
unique_id: persistent_notifications_on
name: Persistent notifications on
icon: mdi:bell
state_class: total
state: |
  {{ this.attributes.entities | count }}
attributes:
  entities: |
    {{ states.persistent_notification | map(attribute="entity_id") | sort | list }}
