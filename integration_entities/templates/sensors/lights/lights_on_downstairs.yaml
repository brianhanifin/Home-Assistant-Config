---
unique_id: lights_on_downstairs
name: Lights on downstairs
icon: mdi:lightbulb-on
state: |
  {{ expand("group.downstairs_lights") | selectattr("state","eq","on") | map(attribute='entity_id') | list | count }}
attributes:
  lights: |
    {{ expand("group.downstairs_lights") | selectattr("state","eq","on") | map(attribute="name") | join(", ") }}
