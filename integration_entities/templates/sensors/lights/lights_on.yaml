---
unique_id: lights_on
name: Lights on
icon: mdi:lightbulb-on
state: |
  {{ expand("group.house_lights") | selectattr("state","eq","on") | map(attribute='entity_id') | list | count }}
attributes:
  lights: |
    {{ expand("group.house_lights") | selectattr("state","eq","on") | map(attribute="name") | join(", ") | replace("Dining","Dining room") | replace("1","") }}
