---
unique_id: family_present
name: Family Present
icon: mdi:cellphone
state_class: total
state: |
  {{ this.attributes.who | count }}
attributes:
  who: |
    {{ states.person | selectattr('state','eq','home') | map(attribute='name') | join(", ")  }}
