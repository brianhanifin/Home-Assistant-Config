---
unique_id: light_count
name: Light count
icon: mdi:lightbulb-multiple
state_class: total
state: |
  {{ states.light|map(attribute="entity_id")|reject("is_hidden_entity")|list|count }}
attributes:
  attribution: State excludes hidden bulbs.
  hidden: |
    {{ states.light|map(attribute="entity_id")|select("is_hidden_entity")|list|count }}
