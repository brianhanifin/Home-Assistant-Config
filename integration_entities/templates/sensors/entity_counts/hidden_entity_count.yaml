---
unique_id: hidden_entity_count
name: Hidden entity count
icon: mdi:eye-off
state_class: total
state:  |
  {{ this.attributes.entities|default("")|count }}
attributes:
  entities: |
    {%- set entities = states|map(attribute="entity_id")|select("is_hidden_entity")|sort|list|replace("[","")|replace("]","")|replace("'","")  %}    {{ entities.split(", ") }}
