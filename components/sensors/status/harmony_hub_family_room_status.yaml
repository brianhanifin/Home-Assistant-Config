---
platform: template
sensors:
  harmony_hub_family_room_status:
    value_template: >
      {%- if states('device_tracker.harmony_hub_family_room') -%}
        {%- if is_state('device_tracker.harmony_hub_family_room', 'home') -%}
          Online
        {%- else -%}
          Offline
        {%- endif -%}
      {%- else -%}
        Unknown
      {%- endif %}
    friendly_name: Harmony Hub Family Room Status