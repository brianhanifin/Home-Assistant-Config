---
platform: template
sensors:
  ecobee_granny_flat_hold_mode:
    friendly_name: "Temp Hold Mode"
    icon_template: >-
      {% if is_state('states.climate.granny_flat.attributes.away_mode', 'on') %}
        mdi:airplane-takeoff
      {% else %}
        mdi:airplane-landing
      {% endif %}
    value_template: "{{ states.climate.granny_flat.attributes.away_mode }}"