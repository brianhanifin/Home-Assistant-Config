---
card:
  type: vertical-stack
  cards:
    - type: custom:weather-card
      entity: weather.dark_sky

    - type: custom:state-switch
      entity: binary_sensor.today_rain
      states:
        "on":
          type: picture-entity
          entity: camera.radar
        # "off":
        #   type: markdown
        #   content: "No rain."
