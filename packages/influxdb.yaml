---
# The influxdb component makes it possible to transfer all state 
# changes to an external InfluxDB database.
#
# This is useful to examine sensor data patterns in external tools
# like Grafana.
#
# https://www.home-assistant.io/components/influxdb/
#
influxdb: !include_dir_merge_named /config/influxdb