---
# The downloader component provides a service to download files. It will 
# raise an error and not continue to set itself up when the download directory 
# does not exist. The directory needs to be writable for the user that is 
# running Home Assistant.
#
downloader:
  download_dir: www/downloads