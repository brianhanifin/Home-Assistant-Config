---
# CONFIGURATION VARIABLES
#
# ffmpeg_arguments
# (string)(Optional)Extra options to pass to ffmpeg, e.g., image quality or 
# video filter options.
#
# scan_interval
# (integer)(Optional)How frequently to query for new video in seconds.
# Default value: 90
#
# Note: To be able to playback the last capture, it is required to install the 
# ffmpeg component. Make sure to follow the steps mentioned at FFMPEG documentation.
platform: ring