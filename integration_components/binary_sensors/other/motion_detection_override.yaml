---
platform: template
sensors:
  motion_detection_override:
    friendly_name: Motion Detection Override
    entity_id: input_boolean.motion_detection_mode
    value_template: "{{ is_state('input_boolean.motion_detection_mode','off') }}"
