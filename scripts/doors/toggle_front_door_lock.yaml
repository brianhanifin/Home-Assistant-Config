toggle_front_door_lock:
  sequence:
    - service_template: >-
        {% if is_state('lock.front_door', 'locked') -%}lock.unlock
        {%- else -%}lock.lock
        {%- endif %}
      entity_id: lock.front_door