lights_flash_hue:
  sequence:
    - delay: "0:00:01"
    - service: light.turn_on
      data_template:
        entity_id: "{{ entity_id }}"
        flash: "short"

    - delay: "0:00:01"
    - service: light.turn_on
      data_template:
        entity_id: "{{ entity_id }}"
        flash: "short"

    - delay: "0:00:01"
    - service: light.turn_on
      data_template:
        entity_id: "{{ entity_id }}"
        flash: "short"
