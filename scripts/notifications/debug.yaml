---
sequence:
  - service: persistent_notification.create
    data_template:
      title: "{{ title|default('Debug') }}"
      message: "{{ message }}"
      notification_id: "{{ id|default('debug') }}"
