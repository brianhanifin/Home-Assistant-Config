---
ecobee_away_and_hold:
  sequence:
    - service: climate.set_away_mode
      data:
        entity_id: climate.home
        away_mode: true
    - service: climate.set_away_mode
      data:
        entity_id: climate.playroom
        away_mode: true