  >-
  {%- if entities is not string -%}
    {%- set temp = 'media_player.kitchen' -%}
  {%- else -%}
    {%- set temp = entities|trim|replace(' ', '') -%}
  {%- endif -%}
  {{ temp|replace(' ,',',')|replace(',',', ')|trim }}