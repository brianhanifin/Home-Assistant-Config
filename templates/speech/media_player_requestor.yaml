  >
  {%- if states('sensor.last_alexa') is not string -%}
    {%- set media_player = 'media_player.kitchen' -%}
  {%- else -%}
    {%- set media_player = states('sensor.last_alexa') -%}
  {%- endif -%}