  >
  {%- macro get_media_player(media_player) -%}
    {%- if media_player is string -%}
      {{ media_player|trim|replace(' ', '') }}
    {%- elif media_player is sequence -%}
      {{ media_player|join(', ') }}
    {%- else -%}
      {{ 'media_player.kitchen' }}
    {%- endif -%}
  {%- endmacro -%}
  {{ get_media_player(media_player) }}